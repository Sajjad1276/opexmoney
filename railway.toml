[build]
builder = "nixpacks"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
startCommand = "uvicorn admin.main:app --host 0.0.0.0 --port $PORT --workers 1 --loop uvloop --http httptools"
